parameters:
  cuVer: ''

steps:
  - task: AmazonWebServices.aws-vsts-tools.S3Upload.S3Upload@1
    displayName: 'Upload ${{ parameters.cuVer }} wheel to S3'
    inputs:
      awsCredentials: 'Pytorch S3 bucket'
      bucketName: 'pytorch'
      sourceFolder: 'windows/output/${{ parameters.cuVer }}'
      globExpressions: '*.whl'
      targetFolder: 's3://pytorch/whl/nightly/${{ parameters.cuVer }}/'
      fileAcl: 'public read'
      flattenFolders: 'true'
