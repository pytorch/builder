name: Validate docker image

on:
  pull_request:
    branches:
    - '*'
jobs:
  build:
    runs-on: ubuntu-latest
    
    steps:
    - name: print a statement
      run: echo "Validating docker image."

